// Go also Golang is a compiled language.

package main						//pakage installation is done by :  pakage keyword before the "pakage name"

import "fmt"						/* import is used to include code from other packages.
									Here fmt stands for formating for input and output. */

func main() {						// using "main" is to declare the code lines are executable.
	fmt.Println("Hello World!")		//Hence there will be no need to call function explicitly for each line of exection.
}